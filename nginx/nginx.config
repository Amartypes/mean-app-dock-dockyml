server {
    listen 80;
    server_name _;

    location / {
        proxy_pass http://<FRONTEND_CONTAINER_IP>:80;
    }

    location /api/ {
        proxy_pass http://<BACKEND_CONTAINER_IP>:8080;
    }
}
